#include "stdio.h"
#include "raylib.h"

void raydrawboard(){

    InitWindow(800, 600, "my Game");

    SetTargetFPS(60);

    while(!WindowShouldClose()){

        BeginDrawing();
        ClearBackground(RAYWHITE);

    }
    
}